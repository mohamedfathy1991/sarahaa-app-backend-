
  export function generateopt(min,max){
      return Math.floor(Math.random()*(max-min+1)+min)
}

export const generateOTP = (min, max) => {
      return Math.floor(Math.random() * (max - min + 1)) + min;
    };